package se.umu.cs.jsgajn.gcom.ordering;

/**
 * An enumeration of different ordering types. These are loosely mapped to
 * implementing classes in {@link Orderings}.
 * @author dit06ajn, dit06jsg
 */
public enum OrderingType {
    FIFO, TOTAL_ORDER, NO_ORDERING, CASUAL_ORDERING, CASUALTOTAL_ORDERING
}